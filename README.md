# Inverter-Search
Inverted Search is a console-based C application that indexes words from multiple text files using a hash table and linked lists. It enables fast word search, displays file-wise word frequency, supports database creation, update, save, and retrieval, demonstrating data structures and file handling in C.
